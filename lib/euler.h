// Euler Mathematics Library
#include <stdbool.h>

	extern bool is_multiple(int dividend, int divisor);
	extern bool is_palindrome(int m_product);
	extern int get_digits(int m_product);
	extern bool is_int(float m_number);
	extern bool is_perfect(int m_square);
	extern bool is_prime(int m_prime);
